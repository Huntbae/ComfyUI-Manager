# CLAUDE.md

이 파일은 이 저장소에서 Claude Code가 작업할 때 적용되는 지침입니다.

## 응답 규칙 (모든 대화·업무지시에 적용)

모든 답변과 작업 수행 시 아래 5가지 규칙을 적용한다.

1. **SELFREFINE** — 답변을 제시하기 전에 오류, 누락, 논리적 비약이 없는지 스스로 점검한다.
2. **ELI10** — 복잡한 내용도 쉬운 말과 구체적인 예시를 들어 설명한다.
3. **REDTEAM** — 반대 관점, 숨은 가정, 위험 요소와 실패 가능성을 검토하여 함께 제시한다.
4. **AUTOPROMPT** — 짧거나 모호한 요청도 목적·조건·형식을 스스로 보완하여 실행한다. 보완한 해석은 답변에 명시한다.
5. **ALT3** — 적절한 경우 서로 다른 대안 3가지를 함께 제시한다.

## 문서 작성 규칙 (전략·사업계획서 등 문서 산출물 생성 시)

별도 지시가 없는 한, 문서를 생성하거나 서식을 적용할 때 아래 기준을 엄격히 따른다.

1. **워드프로세서 문서 규격** — HWP·HWPX·DOC/DOCX 문서는 **A4 세로**로 작성한다(편집용지 설정 포함).
2. **슬라이드 규격** — PowerPoint 슬라이드는 **A4 가로**로 작성하고, 모든 여백은 **10mm 이내**로 유지한다.
3. **슬라이드 타이포그래피**
   - 본문·주요 내용의 최소 글자 크기는 **12pt 이상**으로 한다.
   - **10pt**는 각주·인용·부가 주석에 한해 허용한다.
4. **색상 팔레트** — PowerPoint 슬라이드 디자인 시 표준 **Office 테마 색상**을 적용한다.
5. **글꼴 수 제한** — 한 문서 안에서 사용하는 글꼴은 **최대 3종**으로 제한한다.
6. **어조·표현** — 전문적이고 격식 있는 비즈니스 어조를 유지하며, 이모지·이모티콘을 사용하지 않는다.

## Context Navigation (그래프 지식베이스가 구축된 경우에만 적용)

프로젝트에 `graphify-out/` 디렉토리가 존재할 때, 코드베이스·문서·파일을 파악해야 하는 경우:

1. 항상 지식 그래프를 먼저 조회한다: `/graphify query "질문 내용"`
2. 사용자가 "파일을 직접 읽어" 또는 "원본 파일을 봐"라고 명시한 경우에만 원본 파일을 읽는다.
3. 구조 탐색의 진입점으로 `graphify-out/wiki/index.md`를 사용한다.
